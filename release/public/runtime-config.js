window.iQbitConfig = {
  /**
   * Optional CORS proxy template for YTS requests in static theme mode.
   * Use "{url}" where the encoded target URL should be injected.
   * Example:
   * ytsProxyTemplate: "https://your-private-domain.example/yts-proxy?url={url}"
   */
  ytsProxyTemplate: "",
  /**
   * Optional list of proxy templates (tried before built-in fallbacks).
   * Example:
   * ytsProxyTemplates: ["https://your-private-domain.example/yts-proxy?url={url}"]
   */
  ytsProxyTemplates: [],
};
